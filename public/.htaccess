<IfModule mod_rewrite.c>
    RewriteEngine On

    # Nếu file hoặc thư mục đã tồn tại thì không rewrite
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f

    # Chuyển tất cả các request khác về index.php
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
